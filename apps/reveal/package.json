{
  "name": "@wonderlandlabs-pixi-ux/reveal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3002",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3002",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vite": "^7.1.11",
    "yaml": "^2.4.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
